import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  footer: {
    alignItems: "center",
    padding: 20,
    marginTop: 20,
  },
  footerText: {
    fontSize: 14,
    marginBottom: 6,
  },
});
